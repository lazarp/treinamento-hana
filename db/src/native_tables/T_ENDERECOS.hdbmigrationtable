== version = 2
COLUMN TABLE "T_ENDERECOS" (
    "CLIENTE_ID" INTEGER NOT NULL,
    "ENDERECO_ID" INTEGER NOT NULL,
    "LOGRADOURO" NVARCHAR(255),
    "NUMERO" INTEGER,
    "COMPLEMENTO" NVARCHAR(255),
    "CEP" NVARCHAR(9),
    PRIMARY KEY ("CLIENTE_ID", "ENDERECO_ID")
)

== migration = 2
ALTER TABLE "T_ENDERECOS"
ADD ("CEP" NVARCHAR(9));