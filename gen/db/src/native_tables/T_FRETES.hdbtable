COLUMN TABLE T_FRETES(
    CEP NVARCHAR(9) NOT NULL,
    FRETE_UNIDADE_PESO DOUBLE,
    PRIMARY KEY (CEP)
)
WITH ASSOCIATIONS(
    ONE TO MANY JOIN T_ENDERECOS AS ENDERECOS ON (ENDERECOS."CEP" = "CEP")
)